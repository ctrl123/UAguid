<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ctrl123.uaguid">

    <application
        android:allowBackup="true"
        android:icon="@drawable/uaguid_logotest5"
        android:label="@string/app_name"
        android:roundIcon="@drawable/uaguid_logotest5"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Carte" />
        <activity android:name=".Crous" />
        <activity android:name=".Scolaire" />
        <activity android:name=".Sport"></activity>
    </application>

</manifest>